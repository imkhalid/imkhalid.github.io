<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme using Material 3 -->
    <style name="Theme.Cashlytics" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <!-- Splash screen theme -->
    <style name="Theme.Cashlytics.Splash" parent="Theme.SplashScreen">
        <item name="postSplashScreenTheme">@style/Theme.Cashlytics</item>
    </style>
</resources>
